#include <stdlib.h>
#include <stdio.h>
/**
 * main - Prints all single digit numbers
 *
 * Description: Use the main function to print all single digit numbers
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%d", a);
	}
	printf("\n");
	return (0);
}
