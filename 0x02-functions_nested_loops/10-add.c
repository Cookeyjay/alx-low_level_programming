#include "main.h"
/**
 * add - adds two integers and returns the result
 * @sum: adds both integers
 * @n1: first integer to be added
 * @n2: second integer to be added
 * Return: result
 */
int add(int n1, int n2)
{
	sum;

	add(n1, n2);
	sum = n1 + n2;
	return (sum);
}
